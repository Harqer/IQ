-r requirements-hybrid.txt

# Mamba-3 itself is installed separately with --no-build-isolation so pip does not
# create an isolated build environment with the wrong Torch. Run:
#   bash experiments/architecture_v2/install.sh
